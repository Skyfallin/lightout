import React, { useCallback } from 'react';
import { Pressable, Text, StyleSheet, FlatList as RNFlatList } from 'react-native';

import { Colors } from '../config';

export const FlatList = ({
    data,
    keyExtractor,
    renderItem,
    ...otherProps,
  }) => {
    return (
      <View
        style={
        }
      >
        <RNFlatList
        />
      </View>
    );
  };